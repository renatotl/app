// Vamos até `src/types` e criaremos um arquivo chamado `QueryKey.ts`:

export enum QueryKey {
    LOGIN = "login",
    PRODUCTS = "products",
    TABLES = "tables",
    USERS = "users",
  }


  // lib reactQuery 