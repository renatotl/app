
//Ele será responsável por guardar os alias que fazem referência às rotas da nossa aplicação, com o seguinte código:
export enum RoutePath {
    HOME = '/',
    LOGIN = '/login',
    SETTINGS = '/settings',
    SETTINGS_PRODUCTS = 'products',
    SETTINGS_TABLES = 'tables',
    SETTINGS_USERS = 'users',
  }